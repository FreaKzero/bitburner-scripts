import { findElement, goCity, goLocation, goSidebar } from "./lib/ui";
import { fromFormat, replaceAll } from "./lib/utils";

/** @param {import(".").NS } ns */
export async function main(ns) {
 

    ns.tprintRaw(  fromFormat('200.000k'));

}